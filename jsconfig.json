// jsconfig.json  (alla radice del progetto)
{
  "compilerOptions": {
    "baseUrl": ".", // ← DÌ a TypeScript/Next qual è la root
    "paths": {
      "@/*": [
        "src/*"
      ]
    } // niente ./ davanti
  }
}